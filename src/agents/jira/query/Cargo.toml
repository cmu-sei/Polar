[package]
name = "jira-queries"
version.workspace = true
edition.workspace = true
license = "MIT"

[dependencies]
jira-schema = { path = "../schema" }
# serde = {version = "1", features = ["derive"]}
# chrono = { version = "0.4.39", features = ["serde", "rkyv-32"]}
rkyv = { workspace = true }

[build-dependencies]
cynic-codegen = { version = "3.9.1", features = ["rkyv"] }
