[package]
name = "gitlab-queries"
version.workspace = true
edition.workspace = true
license = "MIT"

[dependencies]
cynic = { version = "3.12.0", features = ["rkyv"]}
gitlab-schema = { path = "../schema" }
# serde = {version = "1", features = ["derive"]}
# chrono = { version = "0.4.39", features = ["serde", "rkyv-32"]}
rkyv = { workspace = true }

[build-dependencies]
cynic-codegen = { version = "3.12.0", features = ["rkyv"] }
