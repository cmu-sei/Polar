# Git Commit Message Hook in Rust

This project provides a `commit-msg` Git hook that enforces [Conventional Commits](https://www.conventionalcommits.org).

## Usage

1. Build and install the hook:

   ```bash
   ./install-hook.sh
