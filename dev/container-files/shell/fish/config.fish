# ~/.config/fish/config.fish  (optional)

# pull in the global init scripts—only needed if you ever comment out
# the lines in /etc/fish/conf.d/* that already source them
source /etc/fish/shellInit.fish
source /etc/fish/interactiveShellInit.fish

# personal tweaks that are truly per-user
#set -gx EDITOR nvim
#alias gs 'git status'
